# NOTE: torch / torchvision are pinned to a CUDA-12.1 build that works on
# Oscar's GPU drivers. Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1
torchvision==0.20.1
transformers>=4.49.0
datasets>=2.17
accelerate
Pillow>=10.0
tqdm
nltk
rouge-score
sentence-transformers<3.0
qwen-vl-utils
numpy
